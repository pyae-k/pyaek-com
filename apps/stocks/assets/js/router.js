/**
 * Router — simplified for single-page dashboard.
 * No tab-based routing. Stock detail is handled by clicking watchlist rows.
 * Kept for backward compatibility.
 */

export function initRouter() {
  // No-op: everything is on one page now
}

export function navigate(path) {
  // No-op: everything is on one page now
}
