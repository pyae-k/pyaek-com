/**
 * Formula column — DuckDB SQL expression mode only.
 * The Excel-style formula parser has been removed in favor of raw SQL expressions
 * with clickable column and function support in the UI dialog.
 *
 * This file is kept as a minimal re-export for backward compatibility of the
 * module path; all formula-related types and functions have been removed.
 */
