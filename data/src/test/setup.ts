// Vitest global setup. Pure logic tests (formula translator, CTE builder,
// global-setup generator, base-path, project migration, step buildSql) run in
// the node environment and need no DOM. Add shared matchers/globals here as
// the suite grows.
export {};